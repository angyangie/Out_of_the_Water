import { isShipStart } from 'out-of-the-water/helpers/is-ship-start';
import { module, test } from 'qunit';

module('Unit | Helper | is ship start');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isShipStart([42]);
  assert.ok(result);
});
