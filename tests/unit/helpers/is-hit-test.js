import { isHit } from 'out-of-the-water/helpers/is-hit';
import { module, test } from 'qunit';

module('Unit | Helper | is hit');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isHit([42]);
  assert.ok(result);
});
