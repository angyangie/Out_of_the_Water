import { shipsAndShots } from 'out-of-the-water/helpers/ships-and-shots';
import { module, test } from 'qunit';

module('Unit | Helper | ships and shots');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = shipsAndShots([42]);
  assert.ok(result);
});
